.DS_Store

# Local, unverified material. Keep it on the workstation until provenance and
# permission to publish it to the private project history are confirmed.
/social/

# Local build dependencies and design-review snapshots.
node_modules/
.impeccable/critique/

# Secrets and local renders.
.env
.env.*
!.env.example
content/production/html-posters/out/
